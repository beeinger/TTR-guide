import styled from "@emotion/styled";
import dynamic from "next/dynamic";

export default function DateRange() {
  return <div></div>;
}

const SelectDateRangeContainer = styled.div`
  grid-area: date;
  justify-self: center;
  align-self: center;
`;
